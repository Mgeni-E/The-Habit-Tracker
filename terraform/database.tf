# =============================================================================
# Database Configuration - Using Docker PostgreSQL
# =============================================================================
#
# Note: PostgreSQL is now running as a Docker container on the VM
# No Azure PostgreSQL resources needed
# Database connection details are handled in docker-compose.yml
#
